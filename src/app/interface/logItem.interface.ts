export interface LogItem {
  vehicleNumber: string;
  date : string;
  totalAmount : number;
  packageName : string;
  oilChanged : boolean;
  airFilterChanged : boolean;
  preMillage : number;
  nextMillage : number;
  nextDate : string;
}
